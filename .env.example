# Local development: create a copy as .env and add your Brevo API key.
# The key is only used by the local API server (scripts/local-api.cjs), never in the browser.
#
# BREVO_API_KEY=your_brevo_api_key_here
#
# Production (Vercel): set BREVO_API_KEY in Project → Settings → Environment Variables.
